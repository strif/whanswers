<%= form_for(@topic_following) do |f| %>
  <% if @topic_following.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic_following.errors.count, "error") %> prohibited this topic_following from being saved:</h2>

      <ul>
      <% @topic_following.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
