<%= form_for(@question_topic) do |f| %>
  <% if @question_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_topic.errors.count, "error") %> prohibited this question_topic from being saved:</h2>

      <ul>
      <% @question_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
